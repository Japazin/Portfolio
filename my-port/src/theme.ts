import { createTheme } from "@mui/material";


const theme = createTheme({
  palette: {
    primary: {
      main: "#ffff",
    },
    secondary: {
      main:"#ffff",
    },
  },
  typography: {
    fontFamily:"Segoe UI"
  }
});
export default theme